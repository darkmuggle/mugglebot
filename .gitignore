# Rust
/target
Cargo.lock.bak

# Local, user-specific config (secrets live in the Keychain, but this still holds
# your channels/handles/paths). Ship config.example.toml instead.
/config.toml

# Local data (SQLite DB, etc.)
/data
*.sqlite
*.sqlite-*

# Frontend
ui/node_modules
ui/dist

# Tilt
.tiltbuild

# macOS
.DS_Store
